{% extends 'base.html.twig' %}

{% block title %}Brand
{% endblock %}

{% block body %}
	<h1>Brand</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ brand.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ brand.name }}</td>
			</tr>
			<tr>
				<th>Country</th>
				<td>{{ brand.country }}</td>
			</tr>
			<tr>
				<th>Country</th>
			</tr>
			<tr>
				{% for model in brand.models %}
					<tr>
						<td>{{ model.name }}</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="4">no records found</td>
					</tr>
				{% endfor %}
			</tr>
		</tbody>
	</tr>
</tbody></table><a href="{{ path('app_brand_index') }}">back to list</a><a href="{{ path('app_brand_edit', {'id': brand.id}) }}">edit</a>{{ include('brand/_delete_form.html.twig') }}{% endblock %}
