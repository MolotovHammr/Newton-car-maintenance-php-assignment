{% extends 'base.html.twig' %}

{% block title %}ScheduledMaintenanceJob{% endblock %}

{% block body %}
    <h1>ScheduledMaintenanceJob</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scheduled_maintenance_job.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scheduled_maintenance_job_index') }}">back to list</a>

    <a href="{{ path('app_scheduled_maintenance_job_edit', {'id': scheduled_maintenance_job.id}) }}">edit</a>

    {{ include('scheduled_maintenance_job/_delete_form.html.twig') }}
{% endblock %}
